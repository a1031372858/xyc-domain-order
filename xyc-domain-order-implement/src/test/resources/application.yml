server:
  port: 80
spring:
  application:
    name: xyc-order
  cloud:
    nacos:
      discovery:
        server-addr: 47.116.216.209:8848
      config:
        group: DEFAULT_GROUP
        server-addr: 47.116.216.209:8848
      loadbalancer:
        ribbon:
          enabled: false
  config:
    import: nacos:xyc-order.yaml